version: 1.0
provider:
  name: openfaas
  gateway: http://127.0.0.1:8080
functions:
  test:
    lang: python3-http-debian
    handler: ./test
    image: lolopop/test:latest
    imagePullPolicy: Always  
    replicas: 3
